{
    "name": "genealogy-lambda",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "nodemon --watch .. --watch . --ext js,json --delay 200 --exec node dev_server.js",
        "audit:fix": "npm audit fix",
        "audit:fix:force": "npm audit fix --force"
    },
    "devDependencies": {
        "nodemon": "^3.0.0"
    },
    "main": "handler.js",
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.500.0",
        "express": "^4.18.2",
        "google-auth-library": "^9.0.0",
        "mime-types": "^2.1.35",
        "sqlite3": "^5.0.2"
    }
}